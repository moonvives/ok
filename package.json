{
  "name": "sara-fit-54",
  "version": "1.0.0",
  "description": "Aplicativo personalizado de disciplina alimentar e emagrecimento saudável baseado em avaliação InBody.",
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "build": "rm -rf dist && mkdir -p dist/src && cp index.html dist/index.html && cp src/*.js src/*.css dist/src/",
    "test": "node --test tests/*.test.js",
    "serve": "node scripts/serve.mjs"
  },
  "devDependencies": {}
}
